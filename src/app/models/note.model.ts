export class Note {
    id!: string;
    title!: string
    content!: string
    created!: Date
    modified!: Date
    addedBy!: string
    addedDate!: Date
    updateddBy!: string
    updatedDate!: Date
}

export class AddNote {
    title!: string
    content!: string
    created!: Date
    modified!: Date
    addedBy!: string
    addedDate!: Date
    updateddBy!: string
    updatedDate!: Date
}